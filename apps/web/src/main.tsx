import { useEffect, useState } from "react";
import { createRoot } from "react-dom/client";
import { languages, type Language, type Profile, type Recommendation } from "@workspot/shared";
import { getLanguageConfig, speakText, supportedLanguages } from "./languageConfig";
import { getLocaleCopy } from "./localeRuntime";
import "./styles.css";

const API = import.meta.env.VITE_API_URL ?? "http://localhost:4000";
type Screen = "welcome" | "login" | "language" | "workIntro" | "recording" | "preview" | "interview" | "profile" | "opportunities" | "opportunityDetail" | "advisor" | "progress" | "notifications" | "help";
type NavScreen = Exclude<Screen, "welcome" | "login" | "language" | "workIntro" | "recording" | "preview" | "interview" | "opportunityDetail">;

const emptyProfile: Profile = { currentWork: "Bamboo craft", skills: ["Bamboo weaving", "Hand tools"], craft: "Bamboo craft", mobility: "15 km", intent: "Either", aspirations: "Grow my work and learn new techniques", district: "Kolkata", age: 31 };
const opportunities: Recommendation[] = [
  { id: "bamboo-training", type: "Training", title: "Bamboo Craft Training", provider: "Skill India Digital", location: "8 km away · Kolkata", match: 92, explanation: "Matches your bamboo craft experience, is available near your location, and helps improve your existing skills.", tags: ["NSQF Level 3", "3 months", "Near you"] },
  { id: "artisan-job", type: "Job", title: "Skilled Bamboo Artisan", provider: "Kolkata Handicrafts Co-op", location: "Kolkata · On-site", match: 82, explanation: "Your hand-tool experience and current craft work match the role's core needs.", tags: ["₹15,000–₹20,000/month", "Full time"] },
  { id: "bamboo-business", type: "Self-employment", title: "Start a Bamboo Craft Business", provider: "ODOP cluster support", location: "Local cluster", match: 88, explanation: "Use your current craft experience with tool-kit and market support to grow independently.", tags: ["Tool support", "Financial support"] },
  { id: "solar-training", type: "Training", title: "Solar PV Installation", provider: "Skill India Digital", location: "15 km away", match: 76, explanation: "A modern sector option that builds on your hands-on ability and has growing district demand.", tags: ["Future-ready", "6 months"] }
];

const copy: Record<Language, { nav: string[]; start: string; continue: string; workQuestion: string; workTitle: string; workHint: string; recordingTitle: string; recordingHint: string; previewTitle: string; previewHint: string; type: string; speak: string; send: string; profile: string; opportunities: string; advisor: string; progress: string; help: string; listen: string; typeHere: string; tapSpeak: string; typeAnswer: string; uploadAudio: string; understanding: string; updating: string }> = {
  English: { nav: ["Home", "My Profile", "Opportunities", "WorkSpot AI", "Progress", "Notifications"], start: "Get Started", continue: "Continue", workQuestion: "What kind of work do you currently do?", workTitle: "Tell us about your work", workHint: "You can describe your work in your own words. For example: farming, tailoring, bamboo craft, construction, driving, shopkeeping, or any other work.", recordingTitle: "Tell us about your work", recordingHint: "Speak naturally. You can preview your answer before sending it.", previewTitle: "Your recording", previewHint: "Listen once, then explicitly send your answer when you are ready.", type: "Type", speak: "Speak", send: "Send Answer", profile: "My Work Profile", opportunities: "Opportunities for you", advisor: "WorkSpot Voice Advisor", progress: "My Progress", help: "Need Help?", listen: "Listen to question", typeHere: "Type here...", tapSpeak: "Tap to Speak", typeAnswer: "Type Answer", uploadAudio: "Upload Audio", understanding: "Understanding your answer...", updating: "WorkSpot AI is updating your work profile." },
  Hindi: { nav: ["होम", "मेरी प्रोफ़ाइल", "अवसर", "WorkSpot AI", "प्रगति", "सूचनाएं"], start: "शुरू करें", continue: "आगे बढ़ें", workQuestion: "आप अभी किस तरह का काम करते हैं?", workTitle: "अपने काम के बारे में बताएं", workHint: "अपने काम का वर्णन अपनी भाषा में करें। जैसे खेती, सिलाई, बांस का काम, निर्माण, ड्राइविंग या दुकानदारी।", recordingTitle: "अपने काम के बारे में बताएं", recordingHint: "स्वाभाविक रूप से बोलें। भेजने से पहले अपना उत्तर सुन सकते हैं।", previewTitle: "आपकी रिकॉर्डिंग", previewHint: "एक बार सुनें और तैयार होने पर अपना उत्तर भेजें।", type: "लिखें", speak: "बोलें", send: "उत्तर भेजें", profile: "मेरी कार्य प्रोफ़ाइल", opportunities: "आपके लिए अवसर", advisor: "WorkSpot वॉइस सलाहकार", progress: "मेरी प्रगति", help: "मदद चाहिए?", listen: "सवाल सुनें", typeHere: "यहां लिखें...", tapSpeak: "बोलने के लिए टैप करें", typeAnswer: "उत्तर लिखें", uploadAudio: "ऑडियो अपलोड करें", understanding: "आपके उत्तर को समझ रहे हैं...", updating: "WorkSpot AI आपकी कार्य प्रोफ़ाइल अपडेट कर रहा है।" },
  Bengali: { nav: ["হোম", "আমার প্রোফাইল", "সুযোগ", "WorkSpot AI", "অগ্রগতি", "বিজ্ঞপ্তি"], start: "শুরু করুন", continue: "এগিয়ে যান", workQuestion: "আপনি এখন কী ধরনের কাজ করেন?", workTitle: "আপনার কাজ সম্পর্কে বলুন", workHint: "আপনার কাজ নিজের ভাষায় বলুন। যেমন কৃষিকাজ, সেলাই, বাঁশের কাজ, নির্মাণ, গাড়ি চালানো বা দোকান চালানো।", recordingTitle: "আপনার কাজ সম্পর্কে বলুন", recordingHint: "স্বাভাবিকভাবে বলুন। পাঠানোর আগে রেকর্ডিং শুনতে পারবেন।", previewTitle: "আপনার রেকর্ডিং", previewHint: "একবার শুনে প্রস্তুত হলে উত্তর পাঠান।", type: "লিখুন", speak: "বলুন", send: "উত্তর পাঠান", profile: "আমার কাজের প্রোফাইল", opportunities: "আপনার জন্য সুযোগ", advisor: "WorkSpot ভয়েস উপদেষ্টা", progress: "আমার অগ্রগতি", help: "সাহায্য দরকার?", listen: "প্রশ্ন শুনুন", typeHere: "এখানে লিখুন...", tapSpeak: "বলতে ট্যাপ করুন", typeAnswer: "উত্তর লিখুন", uploadAudio: "অডিও আপলোড করুন", understanding: "আপনার উত্তর বুঝছি...", updating: "WorkSpot AI আপনার কাজের প্রোফাইল আপডেট করছে।" }
};
const questions: Record<Language, string[]> = {
  English: ["What work do you currently do?", "How long have you been doing this work?", "What skills do you use in your work?", "What tools or equipment do you use?", "How much do you usually earn from this work?", "Would you like to improve your current work or learn a new skill?", "How far are you able to travel for work or training?", "Would you prefer a job, your own business, or either?", "What kind of work would you like to do in the future?", "Is there any difficulty or support you need to improve your work?"],
  Hindi: ["आप अभी कौन सा काम करते हैं?", "आप यह काम कितने समय से कर रहे हैं?", "आप अपने काम में किन कौशलों का उपयोग करते हैं?", "आप कौन से औज़ार या उपकरण इस्तेमाल करते हैं?", "आप इस काम से आमतौर पर कितना कमाते हैं?", "क्या आप अपना काम बेहतर करना या नया कौशल सीखना चाहेंगे?", "आप काम या प्रशिक्षण के लिए कितनी दूर जा सकते हैं?", "आप नौकरी, अपना व्यवसाय या दोनों में से क्या पसंद करेंगे?", "भविष्य में आप किस तरह का काम करना चाहेंगे?", "अपने काम को बेहतर बनाने के लिए आपको किस सहायता की जरूरत है?"],
  Bengali: ["আপনি এখন কী কাজ করেন?", "আপনি কতদিন ধরে এই কাজ করছেন?", "আপনার কাজে কোন দক্ষতাগুলি ব্যবহার করেন?", "কোন সরঞ্জাম বা যন্ত্র ব্যবহার করেন?", "এই কাজ থেকে সাধারণত কত আয় হয়?", "আপনি কি বর্তমান কাজ উন্নত করতে বা নতুন দক্ষতা শিখতে চান?", "কাজ বা প্রশিক্ষণের জন্য কত দূর যেতে পারবেন?", "চাকরি, নিজের ব্যবসা নাকি দুটোই পছন্দ করবেন?", "ভবিষ্যতে কী ধরনের কাজ করতে চান?", "কাজ উন্নত করতে আপনার কী সাহায্য প্রয়োজন?"]
};

function App() {
  const [screen, setScreen] = useState<Screen>(() => ({ "/profile": "profile", "/opportunities": "opportunities", "/ai": "advisor", "/progress": "progress", "/notifications": "notifications", "/help": "help" } as Record<string, Screen>)[window.location.pathname] ?? (window.location.pathname.startsWith("/opportunities/") ? "opportunityDetail" : "welcome"));
  const [language, setLanguageState] = useState<string>(() => localStorage.getItem("workspot.language") ?? "English");
  const [profile, setProfile] = useState<Profile>(emptyProfile);
  const [sessionId, setSessionId] = useState<string | null>(null);
  const [mobile, setMobile] = useState("");
  const [otpSent, setOtpSent] = useState(false);
  const [textMode, setTextMode] = useState(false);
  const [answer, setAnswer] = useState("");
  const [recordingSeconds, setRecordingSeconds] = useState(0);
  const [interviewIndex, setInterviewIndex] = useState(1);
  const [processing, setProcessing] = useState(false);
  const [activeOpportunity, setActiveOpportunity] = useState<Recommendation>(() => opportunities.find((item) => window.location.pathname.endsWith(item.id)) ?? opportunities[0]);
  const [filter, setFilter] = useState("All");
  const [saved, setSaved] = useState<string[]>([]);
  const [advisorInput, setAdvisorInput] = useState("");
  const [advisorMessages, setAdvisorMessages] = useState([{ from: "ai", text: "Have you started your training?" }, { from: "worker", text: "Yes, I enrolled last week." }, { from: "ai", text: "How is your training going?" }]);
  const setLanguage = (value: string) => { setLanguageState(value); localStorage.setItem("workspot.language", value); };
  const c = getLocaleCopy(language, copy[language as Language] ?? copy.English);
  const isProduct = !["welcome", "login", "language", "workIntro", "recording", "preview", "interview"].includes(screen);
  useEffect(() => { if (screen !== "recording") return; const timer = window.setInterval(() => setRecordingSeconds((value) => value + 1), 1000); return () => window.clearInterval(timer); }, [screen]);
  async function createSession() { try { const result = await fetch(`${API}/session`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ language }) }); const data = await result.json(); setSessionId(data.id); } catch { setSessionId(null); } }
  function goTo(target: Screen, explicitPath?: string) { setScreen(target); const path = explicitPath ?? (target === "profile" ? "/profile" : target === "opportunities" ? "/opportunities" : target === "advisor" ? "/ai" : target === "progress" ? "/progress" : target === "notifications" ? "/notifications" : target === "help" ? "/help" : target === "opportunityDetail" ? `/opportunities/${activeOpportunity.id}` : "/"); window.history.pushState({}, "", path); window.scrollTo({ top: 0, behavior: "smooth" }); }
  function logout() { const activeSessionId = sessionId; if (activeSessionId) void fetch(`${API}/session/${encodeURIComponent(activeSessionId)}`, { method: "DELETE" }).catch(() => undefined); setSessionId(null); setOtpSent(false); setTextMode(false); setAnswer(""); setRecordingSeconds(0); setInterviewIndex(1); setProcessing(false); setProfile(emptyProfile); setSaved([]); setAdvisorInput(""); setAdvisorMessages([{ from: "ai", text: "Have you started your training?" }, { from: "worker", text: "Yes, I enrolled last week." }, { from: "ai", text: "How is your training going?" }]); goTo("welcome"); }
  function finishAnswer(value: string) { if (!value.trim()) return; setAnswer(value); setScreen("interview"); setInterviewIndex(2); }
  function nextInterview() { setProcessing(true); window.setTimeout(() => { setProcessing(false); if (interviewIndex >= 10) setScreen("profile"); else setInterviewIndex((value) => value + 1); }, 480); }
  function listen(text: string) { speakText(text, language); }
  const currentQuestions = questions[language as Language] ?? questions.English;
  return <div className="app-shell">{!isProduct ? <Onboarding screen={screen} language={language} setLanguage={setLanguage} copy={c} mobile={mobile} setMobile={setMobile} otpSent={otpSent} setOtpSent={setOtpSent} textMode={textMode} setTextMode={setTextMode} answer={answer} setAnswer={setAnswer} recordingSeconds={recordingSeconds} setRecordingSeconds={setRecordingSeconds} interviewIndex={interviewIndex} processing={processing} currentQuestion={currentQuestions[Math.min(interviewIndex - 1, 9)]} onStart={() => goTo("login")} onLogin={() => setOtpSent(true)} onVerify={() => { createSession(); goTo("language"); }} onLanguageContinue={() => goTo("workIntro")} onSpeak={() => { setRecordingSeconds(0); goTo("recording"); }} onStop={() => goTo("preview")} onSend={finishAnswer} onNext={nextInterview} onListen={listen} onBack={() => goTo("workIntro")} /> : <AppFrame screen={screen as NavScreen} language={language} setLanguage={setLanguage} copy={c} profile={profile} setProfile={setProfile} goTo={goTo} onLogout={logout} opportunities={opportunities} filter={filter} setFilter={setFilter} activeOpportunity={activeOpportunity} setActiveOpportunity={setActiveOpportunity} saved={saved} setSaved={setSaved} advisorInput={advisorInput} setAdvisorInput={setAdvisorInput} advisorMessages={advisorMessages} setAdvisorMessages={setAdvisorMessages} />}</div>;
}

function Logo() { return <div className="logo"><span>W</span><div><strong>WorkSpot</strong><small>Your skills. Your work. Your future.</small></div></div>; }
function OnboardingFrame({ children, language, setLanguage }: { children: React.ReactNode; language: Language; setLanguage: (v: Language) => void }) { return <div className="onboarding"><header className="onboarding-header"><Logo /><div className="language-switcher">{languages.map((item) => <button key={item} className={item === language ? "selected" : ""} onClick={() => setLanguage(item)}>{item === "Bengali" ? "বাংলা" : item === "Hindi" ? "हिंदी" : item}</button>)}</div></header>{children}<footer className="simple-footer"><span>WorkSpot · Your skills. Your work. Your future.</span><span>Protected by design · Voice-first · Multilingual</span></footer></div>; }
function Onboarding(p: any) { if (p.screen === "welcome") return <div className="welcome-page"><header><Logo /><button className="ghost-button" onClick={p.onStart}>Sign in</button></header><div className="welcome-grid"><div className="welcome-copy"><span className="eyebrow">AI-POWERED LIVELIHOOD ASSISTANT</span><h1>Find the work that fits <em>you.</em></h1><p>Tell WorkSpot what you do, what you know, and what you want to achieve. Our AI will help you discover training, jobs, and livelihood opportunities.</p><div className="welcome-actions"><button className="primary-button" onClick={p.onStart}>{p.copy.start}<span>→</span></button><button className="secondary-button" onClick={() => document.getElementById("learn-more")?.scrollIntoView({ behavior: "smooth" })}>Learn more</button></div><div className="trust-row"><span>▣ Your information stays protected</span><span>◎ Multilingual</span><span>◉ Voice-first</span></div></div><div className="welcome-art"><div className="art-glow" /><div className="art-orb"><span>W</span></div><div className="art-card art-card-one"><strong>WorkSpot AI</strong><span>Listening in your language</span></div><div className="art-card art-card-two"><span className="mini-score">82</span><div><strong>Work & Progress Score</strong><small>Updated with your progress</small></div></div></div></div><section id="learn-more" className="learn-strip"><div><span className="eyebrow">A SIMPLE CONVERSATION</span><h2>Your work is the starting point.</h2></div><p>WorkSpot listens, understands your experience, and helps you take the next useful step—whether that is better training, a nearby job, or support to grow your own work.</p></section></div>;
  if (p.screen === "login") return <OnboardingFrame language={p.language} setLanguage={p.setLanguage}><div className="center-screen"><div className="auth-card"><span className="eyebrow">WELCOME BACK</span><h1>Welcome to WorkSpot</h1><p>Sign in to continue your journey.</p>{!p.otpSent ? <><label>Mobile number</label><input className="large-input" value={p.mobile} onChange={(e) => p.setMobile(e.target.value)} placeholder="Enter your mobile number" inputMode="tel" /><button className="primary-button" onClick={p.onLogin}>Send OTP <span>→</span></button></> : <><label>Enter the 6-digit OTP</label><input className="large-input otp-input" placeholder="• • • • • •" inputMode="numeric" /><button className="primary-button" onClick={p.onVerify}>Verify and continue <span>→</span></button><div className="auth-links"><button onClick={() => p.setOtpSent(false)}>Change number</button><button>Resend OTP</button></div></>}<button className="whatsapp-button">Continue with WhatsApp <span>↗</span></button><small className="privacy-note">Your information is used only to provide personalized work and training recommendations.</small></div></div></OnboardingFrame>;
  if (p.screen === "language") return <OnboardingFrame language={p.language} setLanguage={p.setLanguage}><div className="center-screen"><div className="language-card"><span className="eyebrow">ONE CONVERSATION, YOUR LANGUAGE</span><h1>Choose your language</h1><p>WorkSpot will communicate with you in your selected language.</p><div className="language-options">{[{ native: "বাংলা", english: "Bengali", value: "Bengali" }, { native: "हिंदी", english: "Hindi", value: "Hindi" }, { native: "English", english: "English", value: "English" }].map((item) => <button key={item.value} className={p.language === item.value ? "language-option active" : "language-option"} onClick={() => p.setLanguage(item.value)}><span className="language-icon">{item.value === "English" ? "A" : item.native.charAt(0)}</span><span><strong>{item.native}</strong><small>{item.english}</small></span><span className="speaker">◉ Preview</span><i>{p.language === item.value ? "✓" : ""}</i></button>)}</div><button className="primary-button" onClick={p.onLanguageContinue}>{p.copy.continue}<span>→</span></button></div></div></OnboardingFrame>;
  if (p.screen === "workIntro") return <OnboardingFrame language={p.language} setLanguage={p.setLanguage}><div className="conversation-shell"><div className="conversation-heading"><span className="eyebrow">WORKSPOT AI · STEP 1 OF 10</span><h1>{p.copy.workTitle}</h1><p>{p.copy.workHint}</p></div><div className="ai-question-card"><div className="ai-card-top"><span className="ai-avatar">W</span><div><strong>WorkSpot AI</strong><small>Speaking in {p.language}</small></div><button className="listen-button" onClick={() => p.onListen(p.copy.workQuestion)}>◉ {p.copy.listen}</button></div><h2>{p.copy.workQuestion}</h2><div className="input-choice"><button className="voice-choice" onClick={p.onSpeak}><span className="voice-icon">◉</span><strong>{p.copy.speak}</strong><small>Use your voice</small></button><button className="type-choice" onClick={() => p.setTextMode(true)}><span>⌨</span><strong>{p.copy.type}</strong><small>Write your answer</small></button></div>{p.textMode && <div className="text-answer"><textarea value={p.answer} onChange={(e) => p.setAnswer(e.target.value)} placeholder={p.copy.typeHere} /><button className="primary-button" onClick={() => p.onSend(p.answer)}>{p.copy.send}<span>→</span></button></div>}<p className="assist-note">Your answer is private. You can correct it at any time.</p></div></div></OnboardingFrame>;
  if (p.screen === "recording") return <OnboardingFrame language={p.language} setLanguage={p.setLanguage}><div className="recording-shell"><span className="eyebrow">WORKSPOT AI · RECORDING</span><h1>{p.copy.recordingTitle}</h1><p>{p.copy.recordingHint}</p><div className="recording-panel"><div className="recording-mic"><span>◉</span></div><strong>Recording...</strong><div className="waveform">{Array.from({ length: 26 }, (_, i) => <i key={i} style={{ height: `${18 + ((i * 17) % 43)}px` }} />)}</div><div className="timer">00:{String(p.recordingSeconds).padStart(2, "0")}</div><div className="recording-actions"><button className="secondary-button" onClick={() => { p.setRecordingSeconds(0); p.onBack(); }}>Cancel</button><button className="stop-button" onClick={p.onStop}>Stop Recording</button></div></div></div></OnboardingFrame>;
  if (p.screen === "preview") return <OnboardingFrame language={p.language} setLanguage={p.setLanguage}><div className="recording-shell"><span className="eyebrow">WORKSPOT AI · PREVIEW</span><h1>{p.copy.previewTitle}</h1><p>{p.copy.previewHint}</p><div className="preview-panel"><div className="audio-player"><button>▶</button><strong>00:{String(p.recordingSeconds || 8).padStart(2, "0")}</strong><span className="audio-line"><i /></span><span>1:00</span></div><div className="preview-actions"><button className="secondary-button" onClick={p.onSpeak}>◉ Record Again</button><button className="primary-button" onClick={() => p.onSend("Voice answer: I work with bamboo craft.")}>{p.copy.send}<span>→</span></button></div><button className="text-button" onClick={() => { p.setTextMode(true); p.onBack(); }}>Type instead</button></div></div></OnboardingFrame>;
  return <OnboardingFrame language={p.language} setLanguage={p.setLanguage}><div className="conversation-shell interview-shell"><div className="interview-top"><div><span className="eyebrow">WORKSPOT AI · BUILDING YOUR WORK PROFILE</span><h1>WorkSpot AI</h1></div><div className="question-counter">Question <strong>{p.interviewIndex}</strong> of 10</div></div><div className="progress-bar"><i style={{ width: `${p.interviewIndex * 10}%` }} /></div>{p.processing ? <div className="processing-card"><div className="processing-orb">W</div><h2>{p.copy.understanding}</h2><p>{p.copy.updating}</p><div className="processing-dots"><i /><i /><i /></div></div> : <><div className="ai-question-card large"><div className="ai-card-top"><span className="ai-avatar">W</span><div><strong>WorkSpot AI</strong><small>{p.language}</small></div><button className="listen-button" onClick={() => p.onListen(p.currentQuestion)}>◉ {p.copy.listen}</button></div><h2>{p.currentQuestion}</h2></div><div className="answer-toolbar"><button className="voice-action" onClick={p.onSpeak}><span>◉</span><strong>{p.copy.tapSpeak}</strong></button><button onClick={() => p.setTextMode(true)}><span>⌨</span> {p.copy.typeAnswer}</button><button><span>▣</span> {p.copy.uploadAudio}</button></div>{p.textMode && <div className="text-answer interview-input"><textarea value={p.answer} onChange={(e) => p.setAnswer(e.target.value)} placeholder={p.copy.typeHere} /><button className="primary-button" onClick={p.onNext}>{p.copy.send}<span>→</span></button></div>}<p className="assist-note centered">Voice is primary. Tap, type, or upload whenever it is easier.</p></>}</div></OnboardingFrame>;
}

function LanguageMenu({ language, setLanguage, onClose }: { language: string; setLanguage: (value: string) => void; onClose: () => void }) { const [query, setQuery] = useState(""); const selected = getLanguageConfig(language); const filtered = supportedLanguages.filter((item) => `${item.name} ${item.nativeName}`.toLowerCase().includes(query.toLowerCase())); return <div className="language-popover" role="dialog" aria-label="Choose your language"><div className="popover-heading"><div><span className="eyebrow">LANGUAGE</span><strong>Choose your language</strong></div><button aria-label="Close language selector" onClick={onClose}>×</button></div><input className="language-search" autoFocus value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search language..." /> <div className="language-list" role="listbox">{filtered.map((item) => <button role="option" aria-selected={item.name === selected.name} className={item.name === selected.name ? "language-row selected" : "language-row"} key={item.code} onClick={() => { setLanguage(item.name); onClose(); }}><span><strong>{item.nativeName}</strong><small>{item.name}</small></span><span>{item.name === selected.name ? "✓" : ""}</span></button>)}</div></div>; }

function ProfileMenu({ goTo, onClose, onLogout }: { goTo: (target: Screen) => void; onClose: () => void; onLogout: () => void }) { useEffect(() => { const close = (event: MouseEvent | KeyboardEvent) => { const outside = event instanceof MouseEvent && !(event.target as Element).closest(".header-popover-wrap"); const escape = event instanceof KeyboardEvent && event.key === "Escape"; if (outside || escape) onClose(); }; document.addEventListener("mousedown", close); document.addEventListener("keydown", close); return () => { document.removeEventListener("mousedown", close); document.removeEventListener("keydown", close); }; }, [onClose]); return <div className="profile-popover" role="menu"><div className="profile-popover-head"><span className="avatar">AD</span><div><strong>Anita Das</strong><small>Bamboo Craft Worker</small></div></div><button role="menuitem" onClick={() => { goTo("profile"); onClose(); }}>My Profile <span>→</span></button><button role="menuitem" onClick={onClose}>Settings <span>→</span></button><button role="menuitem" onClick={() => { goTo("help"); onClose(); }}>Help <span>→</span></button><button className="profile-logout" role="menuitem" onClick={() => { onLogout(); onClose(); }}>Logout <span>↗</span></button></div>; }

function AppFrame(p: any) {
  const c = p.copy;
  const targets: NavScreen[] = ["home", "profile", "opportunities", "advisor", "progress", "notifications"];
  const [languageOpen, setLanguageOpen] = useState(false);
  const [profileOpen, setProfileOpen] = useState(false);
  useEffect(() => {
    const close = (event: KeyboardEvent) => {
      if (event.key === "Escape") {
        setLanguageOpen(false);
        setProfileOpen(false);
      }
    };
    document.addEventListener("keydown", close);
    return () => document.removeEventListener("keydown", close);
  }, []);
  const selected = getLanguageConfig(p.language);
  return <div className="product-layout">
    <aside className="sidebar">
      <Logo />
      <div className="sidebar-label">WORKSPACE</div>
      <nav>{c.nav.map((item: string, index: number) => <button key={item} className={p.screen === targets[index] ? "nav-item active" : "nav-item"} onClick={() => p.goTo(targets[index])}><span>{["⌂", "◎", "▣", "✦", "↗", "◌"][index]}</span>{item}{index === 5 && <b>2</b>}</button>)}</nav>
      <div className="sidebar-bottom"><button className="nav-item" onClick={() => p.goTo("help")}><span>?</span>{c.help}</button><div className="sidebar-user"><span className="avatar">AD</span><div><strong>Anita Das</strong><small>Kolkata, India</small></div><span>•••</span></div></div>
    </aside>
    <main className="product-main">
      <header className="product-header"><div className="mobile-brand"><Logo /></div><div className="breadcrumb">WorkSpot <span>/</span> {p.screen === "profile" ? c.profile : p.screen === "opportunities" ? c.opportunities : p.screen === "advisor" ? c.advisor : p.screen === "progress" ? c.progress : p.screen === "help" ? c.help : "Home"}</div><div className="header-actions"><div className="header-popover-wrap"><button className="header-language" aria-haspopup="dialog" aria-expanded={languageOpen} onClick={() => { setLanguageOpen((value) => !value); setProfileOpen(false); }}>{selected.nativeName}⌄</button>{languageOpen && <LanguageMenu language={p.language} setLanguage={p.setLanguage} onClose={() => setLanguageOpen(false)} />}</div><button className="help-link" onClick={() => p.goTo("help")}>Help</button><div className="header-popover-wrap"><button className="header-avatar" aria-label="Open profile menu" aria-haspopup="menu" aria-expanded={profileOpen} onClick={() => { setProfileOpen((value) => !value); setLanguageOpen(false); }}>AD</button>{profileOpen && <ProfileMenu goTo={p.goTo} onClose={() => setProfileOpen(false)} onLogout={p.onLogout} />}</div></div></header>
      {p.screen === "profile" && <ProfilePage {...p} />}
      {p.screen === "opportunities" && <OpportunitiesPage {...p} />}
      {p.screen === "opportunityDetail" && <OpportunityDetail {...p} />}
      {p.screen === "advisor" && <AdvisorPage {...p} />}
      {p.screen === "progress" && <ProgressPage {...p} />}
      {p.screen === "notifications" && <NotificationsPage {...p} />}
      {p.screen === "help" && <HelpPage {...p} />}
      {p.screen === "home" && <HomePage {...p} />}
      {!(["profile", "opportunities", "opportunityDetail", "advisor", "progress", "notifications", "help", "home"] as string[]).includes(p.screen) && <HomePage {...p} />}
    </main>
    <nav className="mobile-nav"><button onClick={() => p.goTo("home" as Screen)}><span>⌂</span>Home</button><button onClick={() => p.goTo("opportunities")}><span>▣</span>Opportunities</button><button onClick={() => p.goTo("advisor")}><span>✦</span>AI</button><button onClick={() => p.goTo("progress")}><span>↗</span>Work &amp; Progress</button></nav>
  </div>;
}

function PageHeader({ eyebrow, title, subtitle, action }: { eyebrow?: string; title: string; subtitle?: string; action?: React.ReactNode }) { return <div className="page-heading"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1>{subtitle && <p>{subtitle}</p>}</div>{action}</div>; }
function HomePage(p: any) { return <div className="page home-page"><div className="home-hero"><div><span className="eyebrow">GOOD MORNING, ANITA</span><h1>What would you like to work on today?</h1><p>WorkSpot is here to help you find the next useful step in your work journey.</p><div className="home-actions"><button className="primary-button" onClick={() => p.goTo("advisor")}>Ask WorkSpot AI <span>→</span></button><button className="secondary-button" onClick={() => p.goTo("opportunities")}>Explore opportunities</button></div></div><div className="home-hero-orb"><div className="orb-core">W</div><span>Ready to listen</span></div></div><div className="section-row"><div><span className="eyebrow">YOUR SNAPSHOT</span><h2>Keep moving forward</h2></div><button className="text-button" onClick={() => p.goTo("progress")}>View progress →</button></div><div className="snapshot-grid"><StatCard label="Work & Progress Score" value="82" detail="+4 this month" accent="mint" /><StatCard label="Profile completeness" value="90%" detail="2 details left" accent="blue" /><StatCard label="Saved opportunities" value="3" detail="1 closing soon" accent="purple" /></div><div className="home-columns"><div className="surface-card next-step"><div className="card-heading"><div><span className="eyebrow">RECOMMENDED NEXT STEP</span><h2>Continue your training journey</h2></div><span className="badge green">In progress</span></div><p>You enrolled in Bamboo Craft Training last week. Log your first session to keep your profile up to date.</p><div className="mini-progress"><span style={{ width: "42%" }} /></div><div className="progress-meta"><span>42% complete</span><span>3 of 8 sessions</span></div><button className="secondary-button small" onClick={() => p.goTo("progress")}>Update progress <span>→</span></button></div><div className="surface-card opportunity-teaser"><div className="card-heading"><div><span className="eyebrow">MATCHED FOR YOU</span><h2>Bamboo Craft Training</h2></div><strong className="match-pill">92%</strong></div><p>Skill India Digital · 8 km away · 3 months</p><div className="why-line">✓ Matches your existing experience</div><button className="text-button" onClick={() => { p.setActiveOpportunity(opportunities[0]); p.goTo("opportunityDetail", "/opportunities/bamboo-training"); }}>View opportunity →</button></div></div></div>; }
function StatCard({ label, value, detail, accent }: { label: string; value: string; detail: string; accent: string }) { return <div className={`stat-card ${accent}`}><span>{label}</span><strong>{value}</strong><small>{detail}</small></div>; }
function ProfilePage(p: any) { return <div className="page"><PageHeader eyebrow="YOUR WORK JOURNEY" title={p.copy.profile} subtitle="A living profile that grows as WorkSpot learns about your work and progress." action={<button className="primary-button compact" onClick={() => p.goTo("advisor")}>Update with AI <span>✦</span></button>} /><div className="profile-overview surface-card"><div className="profile-person"><span className="large-avatar">AD</span><div><span className="badge green">● Profile Active</span><h2>Anita Das</h2><p>Bamboo Craft Worker · Kolkata, West Bengal</p><div className="profile-tags"><span>বাংলা</span><span>English</span><span>15 km mobility</span></div></div></div><div className="score-display"><div className="score-circle"><strong>82</strong><span>score</span></div><div><h3>Work & Progress Score</h3><p>Based on relevant work, training, and progress information.</p><button className="info-button">i</button></div></div></div><div className="completion-card surface-card"><div><span className="eyebrow">PROFILE COMPLETENESS</span><strong>90%</strong></div><div className="completion-bar"><span style={{ width: "90%" }} /></div><p>Complete two more details to get more precise recommendations.</p><button className="secondary-button small">Complete profile <span>→</span></button></div><div className="section-row"><div><span className="eyebrow">YOUR INFORMATION</span><h2>Everything in one place</h2></div><span className="muted-label">All sections are editable</span></div><div className="detail-grid"><EditableCard title="Personal details" rows={[["Name", "Anita Das"], ["Age", "31"], ["Location", "Kolkata, West Bengal"], ["Languages", "Bengali · English"]]} /><EditableCard title="Work" rows={[["Current occupation", "Bamboo craft worker"], ["Experience", "8 years"], ["Skills", "Bamboo weaving, hand tools"], ["Tools", "Cutter, weaving frame"]]} /><EditableCard title="Work preferences" rows={[["Preference", "Job or self-employment"], ["Preferred location", "Kolkata district"], ["Mobility", "Within 15 km"]]} /><EditableCard title="Education & training" rows={[["Education", "Secondary school"], ["Completed training", "Craft foundation"], ["Current training", "Bamboo Craft Training"]]} /><EditableCard title="Livelihood" rows={[["Income range", "₹10,000–₹15,000"], ["Business activity", "Seasonal orders"], ["Work frequency", "5–6 days / week"]]} /><EditableCard title="Score breakdown" rows={[["Work activity", "85 / 100"], ["Skill proficiency", "78 / 100"], ["Training progress", "90 / 100"], ["Livelihood progress", "76 / 100"]]} /></div></div>; }
function EditableCard({ title, rows }: { title: string; rows: string[][] }) { return <div className="detail-card surface-card"><div className="card-heading"><h3>{title}</h3><button className="edit-button">Edit</button></div>{rows.map(([key, value]) => <div className="detail-row" key={key}><span>{key}</span><strong>{value}</strong></div>)}</div>; }
function OpportunitiesPage(p: any) { const filtered = p.filter === "All" ? p.opportunities : p.opportunities.filter((item: Recommendation) => item.type === p.filter); return <div className="page"><PageHeader eyebrow="PERSONALIZED FOR YOU" title={p.copy.opportunities} subtitle="Based on your skills, experience, location and goals." action={<button className="secondary-button compact">⌕ Search opportunities</button>} /><div className="filter-row">{["All", "Job", "Training", "Self-employment", "Government support"].map((item) => <button key={item} className={p.filter === item ? "filter-chip active" : "filter-chip"} onClick={() => p.setFilter(item)}>{item === "Government support" ? "Schemes" : item === "Job" ? "Jobs" : item}</button>)}</div><div className="opportunities-grid">{filtered.map((item: Recommendation) => <OpportunityCard key={item.id} item={item} onOpen={() => { p.setActiveOpportunity(item); p.goTo("opportunityDetail", `/opportunities/${item.id}`); }} />)}</div></div>; }
function OpportunityCard({ item, onOpen }: { item: Recommendation; onOpen: () => void }) { return <article className="opportunity-card surface-card"><div className="opportunity-visual"><span>{item.type === "Training" ? "◒" : item.type === "Job" ? "▣" : "↗"}</span><strong>{item.match}%<small>match</small></strong></div><span className="type-label">{item.type}</span><h2>{item.title}</h2><p>{item.provider}</p><div className="opportunity-meta"><span>⌖ {item.location}</span><span>{item.tags[1] || "Flexible"}</span></div><div className="why-box"><strong>Why this fits you</strong><span>✓ {item.explanation}</span></div><div className="tag-row">{item.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><button className="secondary-button full" onClick={onOpen}>{item.type === "Job" ? "Apply" : item.type === "Self-employment" ? "Explore" : "View Details"}<span>→</span></button></article>; }
function OpportunityDetail(p: any) { const item = p.activeOpportunity; return <div className="page"><button className="back-button" onClick={() => p.goTo("opportunities")}>← Back to opportunities</button><div className="detail-hero"><div><span className="type-label">{item.type}</span><h1>{item.title}</h1><p>{item.provider} · {item.location}</p><div className="detail-tags">{item.tags.map((tag: string) => <span key={tag}>{tag}</span>)}</div></div><div className="detail-match"><strong>{item.match}%</strong><span>Match for you</span></div></div><div className="detail-layout"><div><section className="surface-card detail-section"><h2>Why WorkSpot recommends this</h2><p className="large-quote">“{item.explanation}”</p><div className="reason-list"><span>✓ Matches your bamboo craft experience</span><span>✓ Available near your preferred location</span><span>✓ Helps you build toward your goals</span></div></section><section className="surface-card detail-section"><h2>What you should know</h2><div className="fact-grid"><div><span>Location</span><strong>{item.location}</strong></div><div><span>Duration</span><strong>{item.type === "Job" ? "Full time" : "3 months"}</strong></div><div><span>Skills needed</span><strong>Bamboo craft, hand tools</strong></div><div><span>Support</span><strong>Training + local guidance</strong></div></div></section></div><aside className="apply-card surface-card"><span className="eyebrow">READY FOR THE NEXT STEP?</span><h2>Keep this opportunity close</h2><p>Save it, ask WorkSpot AI a question, or start your application.</p><button className="primary-button full" onClick={() => p.setSaved((items: string[]) => items.includes(item.id) ? items.filter((id) => id !== item.id) : [...items, item.id])}>{p.saved.includes(item.id) ? "Saved ✓" : item.type === "Job" ? "Apply" : "Save opportunity"}<span>→</span></button><button className="secondary-button full" onClick={() => p.goTo("advisor")}>Ask WorkSpot AI</button><button className="text-button">Contact organization</button></aside></div></div>; }
function AdvisorPage(p: any) { function send() { if (!p.advisorInput.trim()) return; const text = p.advisorInput.trim(); p.setAdvisorMessages((messages: any[]) => [...messages, { from: "worker", text }, { from: "ai", text: "Thanks for sharing. I’ll add that to your progress and help you find the next step." }]); p.setAdvisorInput(""); } return <div className="page advisor-page"><PageHeader eyebrow="YOUR CONVERSATION PARTNER" title={p.copy.advisor} subtitle="Ask anything about your work, training, opportunities, or support." action={<span className="status-pill"><i /> Active</span>} /><div className="advisor-layout"><section className="chat-card surface-card"><div className="chat-header"><div><span className="ai-avatar">W</span><div><strong>WorkSpot AI</strong><small>Responds in {p.language}</small></div></div><button className="listen-button">◉ Voice mode</button></div><div className="messages">{p.advisorMessages.map((message: any, index: number) => <div className={message.from === "ai" ? "message ai-message" : "message worker-message"} key={`${message.text}-${index}`}>{message.from === "ai" && <span className="message-avatar">W</span>}<div><small>{message.from === "ai" ? "WorkSpot AI" : "You"}</small><p>{message.text}</p>{message.from === "ai" && <button className="message-listen">◉ Listen</button>}</div></div>)}</div><div className="chat-input"><button className="mic-round" onClick={() => p.setAdvisorInput("I want to ask about ")}>◉</button><input value={p.advisorInput} onChange={(e) => p.setAdvisorInput(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") send(); }} placeholder="Ask a question in your language..." /><button onClick={send}>→</button></div></section><aside className="suggestion-card surface-card"><span className="eyebrow">QUICK QUESTIONS</span><h2>What can I help with?</h2>{["Training going well", "Check my opportunity", "Need financial support", "Need tool-kit support", "Need market support", "Update my profile", "Talk to a human officer"].map((item) => <button key={item} onClick={() => p.setAdvisorInput(item)}>{item}<span>→</span></button>)}<div className="advisor-note"><span>◉</span><p>Voice is always available. Your answer is only sent when you choose to send it.</p></div></aside></div></div>; }
function ProgressPage(p: any) { return <div className="page"><PageHeader eyebrow="YOUR JOURNEY" title={p.copy.progress} subtitle="See what you have completed and where your next opportunity can take you." action={<button className="secondary-button compact" onClick={() => p.goTo("advisor")}>Update with AI <span>✦</span></button>} /><div className="progress-layout"><section className="surface-card timeline-card"><div className="card-heading"><div><span className="eyebrow">MILESTONES</span><h2>Your work journey</h2></div><span className="badge green">3 completed</span></div><div className="timeline">{[["Profile Created", "Completed", "✓"], ["Training Recommended", "Completed", "✓"], ["Training Started", "Completed", "✓"], ["Training Completed", "In progress", "◉"], ["Work Started", "Upcoming", "○"], ["Income / Profit Progress", "Tracking", "○"]].map(([title, status, icon], index) => <div className={index < 3 ? "timeline-item done" : index === 3 ? "timeline-item current" : "timeline-item"} key={title}><span className="timeline-dot">{icon}</span><div><strong>{title}</strong><small>{status}</small></div>{index === 3 && <span className="timeline-date">42%</span>}</div>)}</div></section><section className="surface-card metrics-card"><div className="card-heading"><div><span className="eyebrow">WORK PERFORMANCE</span><h2>Your metrics</h2></div><button className="info-button">i</button></div><Metric label="Work activity" value="85" detail="Current status" /><Metric label="Skill proficiency" value="78" detail="Developing well" color="blue" /><Metric label="Training completion" value="42" detail="3 of 8 sessions" color="purple" /></section></div><section className="surface-card score-history"><div className="card-heading"><div><span className="eyebrow">UPDATED OVER TIME</span><h2>Work & Progress Score</h2><p>Your score changes as WorkSpot receives new verified progress information.</p></div><div className="current-score"><strong>82</strong><span>+14 since month 1</span></div></div><svg viewBox="0 0 800 230" role="img" aria-label="Score history from 68 to 82"><path d="M30 186 C150 180, 210 142, 330 148 S480 106, 580 112 S690 70, 770 44" fill="none" stroke="#3d9bda" strokeWidth="4" /><path d="M30 186 C150 180, 210 142, 330 148 S480 106, 580 112 S690 70, 770 44 L770 210 L30 210 Z" fill="url(#area)" opacity=".35" /><defs><linearGradient id="area" x1="0" x2="0" y1="0" y2="1"><stop stopColor="#77b9ec" /><stop offset="1" stopColor="#77b9ec" stopOpacity="0" /></linearGradient></defs><circle cx="30" cy="186" r="6" fill="#fff" stroke="#3d9bda" strokeWidth="4" /><circle cx="330" cy="148" r="6" fill="#fff" stroke="#3d9bda" strokeWidth="4" /><circle cx="770" cy="44" r="6" fill="#fff" stroke="#3d9bda" strokeWidth="4" /></svg><div className="chart-labels"><span>Month 1 · 68</span><span>Month 2 · 74</span><span>Month 3 · 82</span></div><div className="change-list"><span>+6 Skill proficiency</span><span>+4 Training progress</span><span>+4 Livelihood progress</span></div></section></div>; }
function Metric({ label, value, detail, color = "" }: { label: string; value: string; detail: string; color?: string }) { return <div className="metric"><span>{label}</span><strong>{value} <small>/100</small></strong><div className={`metric-bar ${color}`}><i style={{ width: `${value}%` }} /></div><small>{detail}</small></div>; }
function NotificationsPage(p: any) { return <div className="page"><PageHeader eyebrow="STAY IN THE LOOP" title="Notifications" subtitle="Useful reminders and updates from your WorkSpot journey." action={<button className="secondary-button compact">Mark all read</button>} /><div className="notification-list">{[["▣", "New opportunity near you", "Bamboo Craft Training is available 8 km away.", "Today", "blue"], ["◒", "Training reminder", "Your training session starts tomorrow.", "Yesterday", "mint"], ["↗", "Profile updated", "Your training progress has been updated.", "2 days ago", "purple"], ["✦", "WorkSpot AI", "Would you like to check new opportunities?", "3 days ago", "orange"]].map(([icon, title, body, date, color]) => <article className="notification-item surface-card" key={title}><span className={`notification-icon ${color}`}>{icon}</span><div><strong>{title}</strong><p>{body}</p></div><time>{date}</time><button>•••</button></article>)}</div></div>; }
function HelpPage(p: any) { return <div className="page"><PageHeader eyebrow="WE ARE HERE WITH YOU" title={p.copy.help} subtitle="Choose the kind of support that feels easiest right now." /><div className="help-grid">{[["✦", "Ask WorkSpot AI", "Get a quick answer about your work, training, or opportunities.", "Voice"], ["☎", "Contact Local Support", "Speak to a support person who knows your district.", "Call"], ["⌖", "Find Nearby Support Center", "See the nearest center for in-person help.", "Location"], ["◎", "Talk to a Human Officer", "Request a call when voice recognition or anything else feels difficult.", "Human support"]].map(([icon, title, body, action], index) => <article className={index === 3 ? "help-card surface-card prominent" : "help-card surface-card"} key={title}><span className="help-icon">{icon}</span><h2>{title}</h2><p>{body}</p><button className="secondary-button">{action}<span>→</span></button></article>)}</div><div className="human-note surface-card"><span>◉</span><div><strong>Voice recognition not working?</strong><p>After a few tries, WorkSpot will always offer tap-based options and human district support.</p></div><button className="text-button">Learn more →</button></div></div>; }

createRoot(document.getElementById("root")!).render(<App />);
